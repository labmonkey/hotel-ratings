{% extends '@web/content/review-fragment.twig' %}

{% block footer %}
    <div class="review-footer">
        <button type="button" class="btn btn-primary btn-sm" data-toggle="modal"
                data-target="#modal-review-form" data-id="{{ review.id }}">
            Moderate
        </button>
    </div>
{% endblock %}