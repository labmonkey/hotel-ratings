<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ pages.web }}">{{ site.name }}</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                {% if user %}
                    <li><p class="navbar-text">Hello, <b>{{ user.firstname }} {{ user.lastname }}</b></p></li>
                    <li>
                        <a href="{{ pages.logout }}">Log out</a>
                    </li>
                    <li><a href="{{ pages.account }}">Account</a></li>
                {% else %}
                    <li><a href="#" data-toggle="modal"
                           data-target="#modal-login">Account</a></li>
                {% endif %}
                <li><a href="{{ pages.admin }}">Admin</a></li>
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>